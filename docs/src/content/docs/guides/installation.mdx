---
title: Installation
description: A guide in my new Starlight docs site.
---
import CodeVariants from '@/components/CodeVariants.astro'

LCAx is available in several coding languages.

In the following you can see how it can be installed in each supported language.

<CodeVariants
    variants={[
        {label: 'Python', code: 'pip install lcax', lang: 'bash'},
        {label: 'TypeScript', code: 'npm install lcax', lang: 'bash'},
        {label: 'Rust', code: 'cargo add lcax', lang: 'bash'},
    ]}
/>
